<Window x:Class="InventoryApp.View.Sync.DlgUpload"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Actividad en proceso" Height="140" Width="400" ResizeMode="NoResize"  WindowStartupLocation="CenterOwner" WindowStyle="SingleBorderWindow"  Closing="Window_Closing" >
    <Grid>
        <Grid.ColumnDefinitions>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" ></RowDefinition>
            <RowDefinition Height="40" ></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Path=Message}" FontSize="11" Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Left" Name="label1"  TextWrapping="WrapWithOverflow" Margin="5,2" />
        <CheckBox Name="chkClose" IsChecked="{Binding Path=JobDone,Mode=TwoWay, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True}" Visibility="Collapsed" TargetUpdated="chkClose_SourceUpdated" SourceUpdated="chkClose_SourceUpdated" ></CheckBox>
        <CheckBox Name="chkManualClose" IsChecked="{Binding Path=ManualClose,Mode=TwoWay, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True}" Visibility="Collapsed" ></CheckBox>
        <ProgressBar IsIndeterminate="True"  Grid.Row="1" Height="20" HorizontalAlignment="Left" Margin="10,10" Name="progressBar1" VerticalAlignment="Top" Width="355"  />

    </Grid>
</Window>
